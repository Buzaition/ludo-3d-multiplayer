{
  "name": "ludo-3d-multiplayer",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Realtime 3D Ludo platform with matchmaking, public rooms, 2v2, bots and points validation analytics",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check client/app.js && node --check client/admin.js && node --check server/server.js && node --check server/game/GameEngine.js && node --check server/rooms/RoomManager.js && node --check server/socket/socketHandlers.js && node --check server/analytics/GithubAnalyticsStore.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1",
    "three": "^0.169.0"
  }
}
